import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Dsick bay - Workplace Health & Wellbeing | Vaedoc",
  description:
    "Dsick bay is a private digital clinic designed to make your staff healthier, reduce sick days, and ease HR workload. It provides instant triage, teleconsults, mental health support, and lab referrals all in one app.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
